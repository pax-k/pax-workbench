# Task 027 Product / Diagnostic Hierarchy Evidence

Date: 2026-07-23
Status: complete
Source under test: repo-local path and Apple-development-signed native app

## Run Label

`task-027-signed-product-diagnostic-hierarchy`

## Implemented Contract

- `src/lib/action-hierarchy.ts` gives every product state one dominant action
  with phase, inspection/mutation classification, consequence, and confirmation
  copy.
- The goal shell renders that action from the existing repository, recovery,
  controller, and collaboration projection. Manual Markdown remains advanced
  inspection rather than task authority.
- Runtime probes, deterministic controller/UI fixtures, raw helpers, setup
  adapter inspection, raw payloads, and troubleshooting are contained in one
  collapsed `Developer tools` disclosure and labeled diagnostic.
- Collaboration connect/disconnect, Viewer inspection, shared confirmation,
  and repair remain product actions. Coordinates, exact bindings, versions, and
  fixtures are progressively disclosed.
- Engineering principles are contextual reference, not a required operating
  destination.
- Repair guidance comes from typed evidence. An unrelated runtime timeout no
  longer guesses macOS Local Network as the cause.

## Verification

- `bun run check`: pass; authority drift, typecheck, 134 frontend tests, and
  production build passed.
- `cargo test --manifest-path src-tauri/Cargo.toml`: pass; 227 native tests.
- `cargo fmt --manifest-path src-tauri/Cargo.toml --check`: pass.
- `git diff --check`: pass.
- `bun run tauri:build:signed`: pass.
- `codesign --verify --deep --strict --verbose=2 <app>`: valid on disk and
  satisfies its designated requirement.
- Signed executable SHA-256:
  `541b1b4c2b31b5dc6cb61a29aa28f9e413b6da859e7a545ece1e93af5aefe180`.

## Signed Manual Trial

The signed app was quit and relaunched before inspection so the new bundle,
not a previously running process, was under test.

- The default surface showed one blue product action with visible consequence
  and confirmation classification.
- Opening `Developer tools` exposed diagnostic-only fixture, helper, setup, and
  runtime controls; its copy states that they do not fulfill product work or
  advance repository authority.
- Reopening the completed Task 019 fixture reinspected repository and recovery
  truth, rendered `Review completed goal` as the only dominant terminal action,
  preserved `Automatic Codex execution started: false`, and started no process.
- Collapsing `Developer tools` restored the product-first view while retaining
  the evidence spine and advanced inspection surfaces.

## Findings Repaired During Verification

- Recovery initially remained dominant after a fresh resolver preview, which
  hid the required execution confirmation. An executable preview now takes
  precedence only after the separate recovery inspection.
- `continueAvailable` initially rendered a passive result-review action and
  stranded the next iteration. It now presents the fresh next-iteration review.
- A non-executable resolver stop inherited an `awaitingConfirmation` fixture
  state. It is now projected as a typed external stop and cannot expose execute.
- `staleTask` and `interrupted` recovery now use the same safe repository
  reinspection action as resumable state without automatic execution.
- Ready-task test repositories were corrected to include the required authority
  baseline instead of bypassing project setup.

## Evidence Classification

- Proved: hierarchy mapping, phase/state precedence, classifications,
  non-executable stop behavior, typed repair routing, progressive diagnostics,
  collaboration detail placement, full frontend/native regressions, signature.
- Manual: signed-native product-first, expanded diagnostics, completed recovery,
  and collapsed diagnostics states operated through accessibility-backed
  computer control.
- Simulated: controller/runtime fixtures, Viewer/shared repair projections, and
  typed failure families.
- Unproved: unaided founder usability, notarized distribution, and production
  release.

## Follow-Up

Task 028 is ready to turn the verified controller result into one product-facing
diff, criteria, checks, evidence, risk, tracker, and next-action receipt.
