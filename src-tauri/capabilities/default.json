{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Build Right Studio main window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
